services:
  app:
    image: tarkov-gear-generator-dev
    build:
      context: .
      dockerfile: dev.Dockerfile
    volumes:
      - ./:/usr/src/app
    ports:
      - 5173:5173
    container_name: tarkov-gear-generator-dev
